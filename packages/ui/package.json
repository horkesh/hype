{
  "name": "@look/ui",
  "version": "0.0.0",
  "private": true,
  "description": "Universal UI primitives built on Tamagui — Stack, Box, Text, Heading, Button, Card, Input, Image, Link, Spinner, Modal, Sheet. Renders as static CSS on web and native primitives on mobile.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
