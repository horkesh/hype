# backend/ is intentionally NOT a workspace — it depends on @specific-dev/framework
# which isn't on the public npm registry. Backend manages its own install + node_modules.
# All cron jobs install tsx globally and run scripts directly (see .github/workflows).
packages:
  - "apps/*"
