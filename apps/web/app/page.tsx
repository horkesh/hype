// Phase 0 placeholder. Phase 2 will replace this with a server-rendered
// home page that fetches today's events + featured venues from Supabase.
export default function Home() {
  return (
    <main style={{ padding: 48, fontFamily: 'system-ui, sans-serif' }}>
      <h1 style={{ fontSize: 32, marginBottom: 8 }}>Look</h1>
      <p style={{ color: '#888' }}>
        Web app scaffolded. Phase 1 wires Tamagui + shared package primitives.
      </p>
    </main>
  );
}
